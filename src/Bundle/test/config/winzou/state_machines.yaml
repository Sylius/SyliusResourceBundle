winzou_state_machine:
    app_poetry_book:
        class: '%app.model.poetry_book.class%'
        graph: app_poetry_book
        states:
            - published
            - cancelled
            - resumed
        transitions:
            cancel:
                from: [published, resumed]
                to: cancelled
            resume:
                from: [cancelled]
                to: resumed
