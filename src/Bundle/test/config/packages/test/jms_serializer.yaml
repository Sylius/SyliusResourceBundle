jms_serializer:
    visitors:
        xml_serialization:
            format_output: '%kernel.debug%'
    metadata:
        auto_detection: false
        directories:
            app:
                namespace_prefix: "App"
                path: "%kernel.project_dir%/config/serialization"
